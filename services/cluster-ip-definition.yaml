apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
    type: ClusterIP
    ports:
      - targetPort: 80 # Port on the Pod
        port: 80  # Port on the Service
    selector:
      app: myapp
      type: backend